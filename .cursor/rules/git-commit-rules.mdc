---
description: 
globs: 
alwaysApply: false
---
# Git提交规范指南

## 提交信息格式

### 基本格式
```
<type>(<scope>): <subject>

<body>

<footer>
```

### 类型（Type）
- `feat`: 新功能
- `fix`: 修复bug
- `docs`: 文档更新
- `style`: 代码格式（不影响代码运行的变动）
- `refactor`: 重构（既不是新增功能，也不是修改bug的代码变动）
- `perf`: 性能优化
- `test`: 增加测试
- `chore`: 构建过程或辅助工具的变动
- `revert`: 回滚到上一个版本

### 范围（Scope）
- 表示影响范围，可选
- 例如：`(core)`, `(ui)`, `(http)`, `(download)`

### 主题（Subject）
- 简短描述，不超过50个字符
- 以动词开头，使用第一人称现在时
- 第一个字母小写
- 结尾不加句号

### 正文（Body）
- 详细描述，可选
- 使用现在时态
- 说明代码变更的动机和与之前行为的对比

### 页脚（Footer）
- 关闭相关issue，可选
- 格式：`Closes #123, #456`

## 提交示例

### 新功能
```
feat(download): 添加多线程下载支持

- 实现3线程并行下载
- 添加进度显示功能
- 支持断点续传

Closes #123
```

### Bug修复
```
fix(http): 修复SSL证书验证失败问题

- 更新证书验证逻辑
- 添加错误处理机制
- 优化重试策略

Fixes #456
```

### 文档更新
```
docs: 更新README文件

- 添加多线程下载使用说明
- 更新编译依赖说明
- 添加性能优化建议
```

### 代码重构
```
refactor(core): 重构下载器核心逻辑

- 提取公共方法到基类
- 优化错误处理流程
- 改进资源管理机制
```

## 分支管理

### 分支命名
- 主分支：`main` 或 `master`
- 开发分支：`develop`
- 功能分支：`feature/功能名称`
- 修复分支：`hotfix/问题描述`
- 发布分支：`release/版本号`

### 分支策略
1. 从`develop`分支创建功能分支
2. 完成功能后合并回`develop`
3. 定期从`develop`创建发布分支
4. 发布分支测试通过后合并到`main`和`develop`
5. 紧急修复从`main`创建`hotfix`分支

## 提交规范检查

### 使用Git Hooks
- 使用`commitlint`检查提交信息格式
- 使用`husky`管理Git Hooks
- 配置示例：
```json
{
  "commitlint": {
    "extends": ["@commitlint/config-conventional"]
  }
}
```

### 代码审查
- 提交前进行代码自检
- 使用`git diff`检查变更
- 确保代码符合项目规范
- 运行单元测试确保功能正常

## 版本管理

### 版本号规范
- 主版本号：不兼容的API修改
- 次版本号：向下兼容的功能性新增
- 修订号：向下兼容的问题修正

### 标签管理
- 使用语义化版本号
- 格式：`v主版本号.次版本号.修订号`
- 示例：`v1.2.3`

## 最佳实践

### 提交频率
- 频繁提交，每个提交专注于单一功能
- 避免大量代码一次性提交
- 保持提交历史的清晰性

### 提交前检查
1. 运行代码格式化工具
2. 执行单元测试
3. 检查代码风格
4. 更新相关文档

### 提交后检查
1. 确认提交信息格式正确
2. 验证代码已正确推送到远程
3. 检查CI/CD流程是否通过

