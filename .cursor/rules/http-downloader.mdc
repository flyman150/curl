---
description: 
globs: 
alwaysApply: false
---
# HTTP下载器项目指南

## 项目概述
这是一个基于libcurl的HTTP文件下载器，支持多线程下载、断点续传和进度显示功能。

## 核心组件

### 主要类
- [HttpDownloader](mdc:http_downloader.cpp) - 主要的下载器类，实现了所有下载功能

### 关键功能
1. 多线程下载
   - 默认使用3个线程并行下载
   - 支持自定义线程数
   - 自动分块下载

2. 断点续传
   - 支持断点续传功能
   - 自动检测已下载文件大小
   - 从断点处继续下载

3. 安全特性
   - SSL证书验证
   - 主机名验证
   - URL格式验证
   - 文件名合法性检查

4. 进度显示
   - 实时显示下载进度
   - 支持多线程进度汇总

## 使用说明

### 编译要求
- C++11或更高版本
- libcurl库
- 支持多线程的操作系统

### 依赖项
- libcurl
- C++标准库
- POSIX系统调用（用于文件操作）

### 编译命令
```bash
g++ -o http_downloader http_downloader.cpp -lcurl -pthread
```

### 运行示例
```bash
./http_downloader <URL> <filename>
```

## 代码结构
- 构造函数：初始化下载器，验证URL和文件名
- download()：执行下载操作
- downloadChunk()：执行分块下载
- getFileSize()：获取远程文件大小
- 各种回调函数：处理下载进度和写入操作

## 注意事项
1. 需要服务器支持Range请求才能使用多线程下载
2. 建议根据文件大小和网络环境调整线程数
3. 确保有足够的磁盘空间
4. 需要适当的文件写入权限

